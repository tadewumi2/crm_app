<div class="customer-card">
  <div class="customer-header">
    <% if customer.image.attached? %>
      <div class="customer-image">
        <%= image_tag customer.image, alt: customer.full_name, class: "customer-photo" %>
      </div>
    <% end %>
    <div class="customer-info">
      <h3 class="customer-name"><%= customer.full_name %></h3>
      <% if customer.email_address.present? %>
        <p class="customer-email">📧 <%= customer.email_address %></p>
      <% else %>
        <p class="customer-email missing">📧 No email provided</p>
      <% end %>
      <% if customer.phone_number.present? %>
        <p class="customer-phone">📞 <%= customer.phone_number %></p>
      <% end %>
    </div>
  </div>
  <% if customer.notes.present? %>
    <div class="customer-notes">
      <strong>Notes:</strong> <%= customer.notes %>
    </div>
  <% end %>
</div>
